<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🔹 Base Theme ของแอป -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/calc_bg</item>
        <item name="android:textColor">@color/calc_text</item>
        <item name="android:buttonStyle">@style/CalcButtonBase</item>
    </style>

    <!-- 🔸 ปุ่มพื้นฐานทั้งหมด -->
    <style name="CalcButtonBase">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:background">@color/calc_surface</item>
        <item name="android:textColor">@color/calc_text</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <!-- ปิดเงา state animation -->
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- 🔹 ปุ่มตัวเลข -->
    <style name="CalcNumber" parent="CalcButtonBase" />

    <!-- 🔹 ปุ่มเครื่องหมาย (เช่น + - × ÷) -->
    <style name="CalcPrimary" parent="CalcButtonBase">
        <item name="android:background">@color/calc_primary</item>
    </style>

    <!-- 🔹 ปุ่มฟังก์ชันรอง (AC, DEL, %) -->
    <style name="CalcSecondary" parent="CalcButtonBase">
        <item name="android:background">@color/calc_surface_2</item>
    </style>

    <!-- 🔹 ปุ่ม = -->
    <style name="CalcEquals" parent="CalcPrimary" />

    <!-- 🔹 ปุ่ม 0 (กลางใหญ่พิเศษ ถ้าต้องการ span 2 ช่อง) -->
    <style name="CalcZero" parent="CalcNumber">
        <item name="android:layout_weight">2</item>
    </style>

</resources>
